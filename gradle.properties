# Use official Kotlin code style (no trailing commas, standard indentation)
kotlin.code.style=official

# Build performance: enable configuration cache, parallel execution, build caching
org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
